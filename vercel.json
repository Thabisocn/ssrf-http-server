{
  "version": 2,
  "builds": [
    {
      "buildCommand": "bun run build",
      "src": "src/app.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["src/**"] }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}